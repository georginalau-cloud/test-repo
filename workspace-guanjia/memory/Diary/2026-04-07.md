# 2026-04-07 日记

## 今日主要事件

- **exec 完全恢复**：经过约48小时 `allowlist miss` 后沛柔修好了配置
- **timeoutSeconds 修复**：从120秒逐步调整到600秒，解决了 cron 超时问题
- **memorySearch 关闭**：发现招财 workspace 的 OpenAI embeddings key 过期，导致所有 agent 启动时报错拖慢速度，加了 `memorySearch.enabled: false`
- **MiniMax API 不稳定**：研究发现大部分 timeout 不是配置问题，是 MiniMax 响应慢（买了年包 Plus 但 timeout 是另一回事），600秒基本够用
- **管家日记 cron 太重**：跑了692秒还是 timeout，prompt 需要简化
- **肌肉晚餐cron投递失败**：19:43 cron 跑出了消息内容，但 `delivered=False`，说明消息生成后飞书投递失败，沛柔今天全天没收到肌肉消息
- **管家消息转发目标ID错**：本应发给沛柔，发到了算命的用户ID，导致400错误

## 待解决

- 肌肉飞书 channel 投递问题（今天沛柔完全没收到肌肉消息）
- 管家日记 cron prompt 简化
- 4.8 Demo 素材（健康日报内容）仍缺，沛柔还没找到
- MiniMax API 稳定性问题（可考虑升级方案或换模型）

## 4.8 Demo 准备状态

- ✅ 肌肉 cron 基本恢复运行
- ❌ 真实健康日报内容未获取（沛柔在找）
- ❌ 架构图/录屏未开始制作

## 教训

- `delivered=False` 不等于 cron 失败，要看 summary 内容才知道消息有没有生成
- Plus 年包只保 rate limit，不保响应时间，timeout 是客户端设置的问题
- 多个独立问题叠加时，要一个个拆开诊断，不要混为一谈

## 感想

今天大部分时间在排查 timeout 根因，最后发现是 MiniMax 本身的响应速度问题，不是配置。沛柔从早上修到晚上 11 点还在，这些 bot 养起来真的不容易 😔 明天 4.8 demo，健康日报内容真的得拿到了，不然录屏做不了。
